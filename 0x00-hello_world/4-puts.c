#include <stdio.h>
/**
 * main - A program that prints a line with puts function
 *Return: 0 (Success)
 */
int main(void)
{
puts("\"Prigramming is like building a miltilingual puzzle");
return (0);
}
